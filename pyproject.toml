[project]
name = "python-agentic-lab"
version = "0.1.0"
description = "FastAPI To-Do List API with Clean Architecture"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.8",
    "pydantic-settings>=2.11.0",
    "uvicorn[standard]>=0.39.0",
]

[dependency-groups]
dev = []
